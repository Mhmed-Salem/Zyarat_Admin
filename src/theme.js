import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: { 
            main: '#19BCDB',
            contrastText: '#fff',
            
        },
        
        secondary: {
            main: '#EC5C42'
        }
    }
});

export default theme;